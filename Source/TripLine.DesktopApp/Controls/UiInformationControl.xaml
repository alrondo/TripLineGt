<UserControl x:Class="TLine.DpSystem.Ui.Configuration.Core.Controls.UiInformationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Name="stackKeys" Margin="5">
            
        </StackPanel>
        <StackPanel Name="stackVals" Margin="5" Grid.Column="1"></StackPanel>
    </Grid>
    <!-- This control has a different theme then the default TLine theme. It has been decided to not define a "TLine Light" theme-->
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml"></ResourceDictionary>
    </UserControl.Resources>
</UserControl>
