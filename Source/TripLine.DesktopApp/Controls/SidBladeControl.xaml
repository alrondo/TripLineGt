<UserControl x:Class="TLine.DpSystem.Ui.Configuration.Core.Controls.SidBladeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding SidFilterOperation}" Grid.Row="0" FontWeight="Bold" FontSize="14" Margin="0,5,0,5"/>
            <ListBox ItemsSource="{Binding SidAddresses}" Grid.Row="1" FontSize="14"/>
        </Grid>
</UserControl>
